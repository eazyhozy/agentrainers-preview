services:
  runner:
    image: python:3.12-slim
    working_dir: /app
    command: bash _run.sh
    volumes:
      - .:/app
      - deps:/usr/local/lib/python3.12/site-packages
      - bin:/usr/local/bin

volumes:
  deps:
  bin:
